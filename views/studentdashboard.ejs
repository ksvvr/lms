<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    
    <%- include('insideHead') %>
</head>

<body class="text-center">
    <%- include('navbar.ejs', { request: request }) %>
    <h1 class="text-xl font-bold mt-2">Hi! <%= userDetail %></h1>
    <p class="">( <%= request.user.email %> )</p>
    <hr>
    <h3>You can enroll in the below courses...</h3>
    <p>Click on any course below to view details...</p>
    <p>Use CTRL+F to find a Course</p>
    <%- include('notifications') %>
    <table class="mx-auto">
        <tr>
            <th>
                Course.No
            </th>
            <th>
                Course Name
            </th>
        </tr>
        <% for(var i=0; i < courses.length ;i++){ %>
            <tr class="bg-sky-300 border hover:bg-sky-400" onclick="window.open('/courses/<%= courses[i].id %>', '_self');" style="cursor: pointer;">
                <td>
                  <%= courses[i].id %>
                </td>
                <td>
                  <%= courses[i].name %>
                </td>
              </tr>           
        <% } %>
    </table>
    <hr class="mt-2">
</body>
 
</html>