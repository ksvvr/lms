<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Course Status</title>
    <%- include('insideHead') %>
</head>

<body class="text-center">
    <%- include('navbar.ejs', { request: request }) %>
    <h2 class="font-bold">Your Course Progress</h2>
    <%- include('notifications') %>
    

    <div>
        <p class="inline-block">Course : </p>
        <p class="font-bold text-3xl inline-block"><%= course.name %></p>
        <p class="block bg-lime-300 hover:bg-lime-400 mt-2" onclick="window.location.href='/course/<%= course.id %>'">Go Back to Course</p>
    </div>
    <p class="font-bold bg-sky-300 mt-2">Completion Percentage: <%= completionPercentage.toFixed(2) %>%</p>
    <% course.Chapters.forEach(chapter=> { %>
        <h3 class="font-bold">
            <%= chapter.name %>
        </h3>
        <% chapter.Pages.forEach(page=> { %>
            <div>
                <span>
                    <%= page.title %>
                </span>
                <% if (page.Completions.length> 0) { %>
                    <span style="color: green;">Completed</span>
                <% } else { %>
                    <span style="color: red;">Not Completed</span>
                <% } %>
            </div>
        <% }); %>
    <% }); %>

    

</body>

</html>